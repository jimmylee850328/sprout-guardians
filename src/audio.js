export class Sound {
 constructor(){this.enabled=true;this.ctx=null;this.volume=.22}
 unlock(){if(!this.ctx){try{this.ctx=new(window.AudioContext||window.webkitAudioContext)()}catch{return}}if(this.ctx.state==='suspended')this.ctx.resume().catch(()=>{})}
 tone(freq,duration=.1,type='sine',volume=.3,delay=0,end){if(!this.enabled||!this.ctx||this.ctx.state!=='running')return;const t=this.ctx.currentTime+delay;const o=this.ctx.createOscillator(),g=this.ctx.createGain();o.type=type;o.frequency.setValueAtTime(freq,t);if(end)o.frequency.exponentialRampToValueAtTime(end,t+duration);g.gain.setValueAtTime(0,t);g.gain.linearRampToValueAtTime(volume*this.volume,t+.008);g.gain.exponentialRampToValueAtTime(.001,t+duration);o.connect(g);g.connect(this.ctx.destination);o.start(t);o.stop(t+duration+.03)}
 play(name){if(name==='pea')this.tone(550,.08,'sine',.18,0,280);else if(name==='mushroom'){this.tone(140,.22,'triangle',.4,0,38);this.tone(75,.18,'sine',.4)}else if(name==='frost'){this.tone(1200,.2,'sine',.13,0,850);this.tone(1600,.2,'sine',.08,.045)}else if(name==='build'){[390,520,780].forEach((f,i)=>this.tone(f,.16,'sine',.4,i*.07))}else if(name==='coin')this.tone(1100,.09,'sine',.14,0,1500);else if(name==='hurt')this.tone(170,.2,'triangle',.4,0,70);else if(name==='start')[330,440,660].forEach((f,i)=>this.tone(f,.18,'triangle',.3,i*.12));else if(name==='win')[523,659,784,1046].forEach((f,i)=>this.tone(f,.35,'sine',.5,i*.15));else if(name==='lose')[392,330,261,196].forEach((f,i)=>this.tone(f,.3,'triangle',.3,i*.2));else this.tone(680,.07,'sine',.22)}
 toggle(){this.unlock();this.enabled=!this.enabled;return this.enabled}
}
